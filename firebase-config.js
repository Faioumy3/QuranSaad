# دليل نظام التنقل بين الصفحات

## نظرة عامة
تم تحسين نظام التنقل بين صفحات التطبيق ليعمل بطريقة احترافية حيث:

## كيفية عمل النظام ✨

### 1️⃣ **الانتقال إلى صفحة جديدة**
- عند الضغط على أي زر للانتقال لصفحة جديدة:
  - ✅ تُخفّى الصفحة السابقة تماماً
  - ✅ تُظهّر الصفحة الحالية فقط
  - ✅ تُضاف الصفحة الجديدة لمكدس الصفحات
  - ✅ يتم التمرير للأعلى تلقائياً

### 2️⃣ **زر الرجوع (goBack)**
- عند الضغط على "رجوع":
  - ✅ تُحذف الصفحة الحالية من المكدس
  - ✅ تُظهّر الصفحة السابقة
  - ✅ يتم التمرير للأعلى تلقائياً
  - ✅ يعمل من أي عمق من الصفحات

مثال على سلسلة التنقل:
```
الرئيسية → دخول المعلم → صفحة الحضور
   ↑                           ↓
   ← ← ← يضغط رجوع يعود للخطوة السابقة
```

### 3️⃣ **الخروج الكامل (logoutAdmin أو logout)**
- عند الضغط على "خروج":
  - ✅ يتم مسح جميع الصفحات من المكدس
  - ✅ يتم التوجيه للصفحة الرئيسية مباشرة
  - ✅ يتم إظهار Footer والصورة الملهمة

## الدوال الرئيسية 🔧

### `navigateToPage(pageId)`
تنقل إلى صفحة معينة وتضيفها للمكدس
```javascript
navigateToPage('dashboardPage');
```

### `goBack()`
تعود للصفحة السابقة
```javascript
// لا تحتاج لمعاملات
goBack();
```

### `goToHome()` أو `showHomePage()`
العودة للصفحة الرئيسية مباشرة
```javascript
goToHome();
```

### `logout()` و `logoutAdmin()`
الخروج الكامل مع العودة للرئيسية
```javascript
logout();      // خروج المعلم
logoutAdmin(); // خروج الإدارة
```

## قائمة الصفحات المتاحة 📄

| معرّف الصفحة | الاسم | نوع |
|---|---|---|
| `homePage` | الصفحة الرئيسية | عامة |
| `loginPage` | دخول المعلم | تسجيل دخول |
| `adminLoginPage` | دخول الإدارة | تسجيل دخول |
| `attendancePage` | صفحة تسجيل الحضور | معلم |
| `dashboardPage` | لوحة التحكم | إدارة |
| `studentsManagementPage` | إدارة الطلاب | إدارة |
| `teacherStudentsPage` | إدارة طلاب المعلمين | إدارة |
| `teacherReportsPage` | تقارير المعلمين | إدارة |
| `backupReportsPage` | النسخ والتقارير | إدارة |

## أمثلة الاستخدام 💡

### مثال 1: تسلسل دخول المعلم
```
1. الضغط على "دخول المعلمين"
   → showLoginPage() → التنقل لصفحة الدخول

2. إدخال البيانات والضغط "دخول"
   → loginTeacher() → showAttendancePage() → التنقل لصفحة الحضور

3. الضغط على "خروج"
   → logout() → showHomePage() → العودة للرئيسية
```

### مثال 2: تسلسل دخول الإدارة
```
1. الضغط على "لوحة التحكم"
   → showAdminLoginPage() → التنقل لصفحة الدخول

2. إدخال كلمة السر والضغط "دخول"
   → loginAdmin() → showDashboardPage() → التنقل للوحة التحكم

3. الضغط على "إدارة الطلاب"
   → showStudentsManagementPage() → التنقل لصفحة إدارة الطلاب

4. الضغط على "رجوع"
   → goBack() → العودة للوحة التحكم

5. الضغط على "خروج"
   → logoutAdmin() → showHomePage() → العودة للرئيسية
```

## نقاط مهمة ⚠️

1. **مكدس الصفحات (Page Stack)**
   - يتم الاحتفاظ بتاريخ الصفحات المزارة
   - يسمح بالعودة للصفحة السابقة مباشرة
   - يُمسح عند العودة للصفحة الرئيسية

2. **الصفحة الرئيسية**
   - تظهر Footer والصورة الملهمة
   - تمسح مكدس الصفحات عند الوصول إليها
   - هي نقطة البداية والنهاية

3. **الخروج**
   - يُغلق جميع الصفحات المفتوحة
   - يُعيد تعيين بيانات المستخدم
   - يعود للصفحة الرئيسية مباشرة

## تحسينات التطبيق ✅

- ✨ واجهة مستخدم نظيفة وبديهية
- 🔄 تنقل سلس بين الصفحات
- 🎯 سهولة التتبع والعودة
- 📱 متوافق مع جميع الأجهزة
- 🚀 أداء عالي وتحميل سريع

---
**تم التحديث**: نوفمبر 2025
**الإصدار**: 2.0 (نظام تنقل محسّن)
